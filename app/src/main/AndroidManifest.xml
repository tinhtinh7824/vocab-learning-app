<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Quyền truy cập mạng -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VocabLearningApp"
        tools:targetApi="31">

        <!-- Màn hình chính (mặc định khi mở app) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Màn hình đăng nhập -->
        <activity android:name=".activities.LoginActivity" />

        <!-- Màn hình đăng ký -->
        <activity android:name=".activities.RegisterActivity" />

        <!-- Khai báo LearningActivity -->
        <activity android:name=".activities.LearningActivity" />


        <!-- Màn hình Flashcard -->
        <activity android:name=".activities.FlashcardActivity" />

        <!-- Màn hình Quiz -->
        <activity android:name=".activities.QuizActivity" />

        <!-- Màn hình danh sách từ vựng -->
        <activity android:name=".activities.VocabularyListActivity" />

        <activity android:name=".TopicDetailActivity"/>

        <activity android:name=".activities.QuizHistoryActivity"/>

    </application>

</manifest>
